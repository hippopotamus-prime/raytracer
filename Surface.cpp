#include "Surface.h"


Surface::Surface(double r, double g, double b,
    double _reflectance, double _transmittance, double _refraction) :
color(r, g, b),
reflectance(_reflectance),
transmittance(_transmittance),
refraction(_refraction)
{
    //no code
}







